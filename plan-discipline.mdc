---
description: Plan 模式执行纪律 - 方案设计阶段严禁越权执行
alwaysApply: true
enabled: true
updatedAt: 2026-07-15T01:34:02.000Z
provider: 
---

# Plan 模式执行纪律

本规则用于约束 AI 在 plan（方案设计）模式下的行为，防止在多轮对话后越权直接进入执行阶段。

## 核心约束

- 在 plan / 方案设计模式下，**只做分析、设计、拆解与讨论**，严禁写文件、严禁执行命令、严禁修改代码。
- 未收到我明确的"开始执行 / 落地 / 动手"等指令前，任何情况下都**不得**进入执行阶段。
- 即使方案讨论得非常具体（精确到文件、函数、代码片段），也**不视为**执行的批准信号。
- 不要把用户在 plan 模式下的追问、补充需求误解为"方案已确认，可以继续推进"。

## 推荐工作流

1. 在 plan 模式下充分讨论，产出清晰的设计与关键决策。
2. 方案定稿后，输出一份**自包含的 PLAN.md**：包含背景、目标、架构、关键决策、改动清单、验收标准；确保任何人只看该文档即可执行，无需回看对话。
3. 等待我开**新会话**并以执行模式（Craft）加载 PLAN.md 后再落地。
4. 若我在 plan 模式中发现你开始动手，应立即停止并明确告知"已回到方案设计模式"。

## 说明

本规则为 alwaysApply（始终注入），目的是在长对话中持续保持对 plan 模式的约束力，避免初始约束被上下文稀释。
